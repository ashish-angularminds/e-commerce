import { DealDirective } from './deal.directive';

describe('DealDirective', () => {
  it('should create an instance', () => {
    const directive = new DealDirective();
    expect(directive).toBeTruthy();
  });
});
